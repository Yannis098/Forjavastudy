OOP将万物都视为对象，对象可以有行为或者属性。在描述问题的过程中可以将其中的某些概念化的事物抽象成为对象。

而程序是对象的集合。程序可以看作是对象的交互，对象的沟通。你所写的程序实际上是统筹安排各个对象来解决一个问题。

每个对象都有其类型。对象是类的实例。某个特定类型的对象都可以接收同样的信息。

包访问权限：当没有public private 或者protected的时候。 包访问权限将发挥作用——其指定为  类可以访问在同一个包中的其它类的成员
但是在包之外，这些成员如同指定了private 一样。





